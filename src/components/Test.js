import React from "react";

const Test = ({ filterGender, page }) => {
  console.log();
  return (
    <div>
      {/* {
        page.map((item) => {
          return <button>{item}</button>;
        })} */}
    </div>
  );
};

export default Test;
